---

- name: Enable HTTP and HTTPS
  firewalld:
    service: "{{ item }}"
    state: enabled
    permanent: yes
    immediate: yes
  with_items: ["http", "https"]

- name: Enable app ports
  firewalld:
    port: "{{ item.value.port }}/tcp"
    state: enabled
    permanent: yes
    immediate: yes
  with_dict: "{{ rails_nginx_server_blocks }}"
  when: item.value.port is defined
